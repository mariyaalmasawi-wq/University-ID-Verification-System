# University-ID-Verification-System
A Python-based system that validates university ID numbers using regular expressions. The program extracts valid IDs from user‑entered sentences, checks their format, removes duplicates, and displays accurate results.
